import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  card: {
    margin: 10,
    padding: 20,
    borderRadius: 18,
  },
  cardTitle: {
    fontSize: 20,
    textAlign: 'center',
    color: '#4D4D4D',
  },
  cardContent: {
    fontSize: 40,
    textAlign: 'center',
  },
})
module.exports.styles = styles
